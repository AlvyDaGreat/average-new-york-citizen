[gd_scene load_steps=3 format=3 uid="uid://m7krihvhvtvc"]

[sub_resource type="CanvasItemMaterial" id="CanvasItemMaterial_xy8xo"]
blend_mode = 1

[sub_resource type="GDScript" id="GDScript_c08pn"]
resource_name = "fadeout"
script/source = "extends Node2D

@onready var h = 0
@onready var v = 0
@onready var a = 1
# Called when the node enters the scene tree for the first time.

func _ready() -> void:
	for i in get_children():
		i.material = material

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	a -= delta * 2.75
	h += delta * 5
	v += delta * 5
	modulate = Color.from_hsv(h,v,1,a)
	if modulate.a <= 0:
		print('haha')
		queue_free()
"

[node name="Dash" type="Node2D"]
material = SubResource("CanvasItemMaterial_xy8xo")
script = SubResource("GDScript_c08pn")
